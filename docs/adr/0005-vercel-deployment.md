# Hono backend deployed to Vercel

The Hono.js backend is deployed to Vercel. Vercel is the natural runtime for the Vercel AI SDK and keeps the deployment platform unified with any future web build of the Expo app. Cloudflare Workers was considered (Hono runs well there) but ruled out to avoid splitting infrastructure.
